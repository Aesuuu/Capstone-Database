<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.capstone_project_redo">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Capstone_Project_Redo">
        <activity android:name=".ReportVendor"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".VendorCarousel" />
        <activity android:name=".ConsumerCarousel" />
        <activity android:name=".LinForgotPass" />
        <activity android:name=".nav.Dashboard" />
        <activity android:name=".nav.Reviews" />
        <activity android:name=".nav.MyAccount" />
        <activity android:name=".category.VendorData" />
        <activity android:name=".ConsumerRegister" />
        <activity android:name=".AddItemActivity$EditProductActivity" />
        <activity android:name=".category.Basic" />
        <activity android:name=".category.Food" />
        <activity android:name=".category.CraftedGoods" />
        <activity android:name=".category.Misc" />
        <activity android:name=".category.ProductData" />
        <activity android:name=".CreateAccountPart1" />
        <activity android:name=".CreateAccountPart2" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".AddItemActivity" />
        <activity android:name=".nav.SRPActivity" />
        <activity android:name=".nav.AboutActivity" />
        <activity android:name=".nav.MyProductsActivity" />
        <activity android:name=".nav.CategoryProduct" />
        <activity android:name=".nav.CategoryVendor" />
        <activity android:name=".MyProfileActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".DrawerBaseActivity" />
        <activity
            android:name=".nav.HomePage"
            android:label="@string/title_activity_home_page"
            android:theme="@style/Theme.Capstone_Project_Redo.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>